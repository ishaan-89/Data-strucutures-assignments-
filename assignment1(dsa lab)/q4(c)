#include<bits/stdc++.h>
using namespace std;
const int maxcols=100;

    void printmatrix(int matrix[][maxcols], int rows, int cols){
        for(int i=0;i<rows;i++){
            for(int j=0;j<cols;j++){
                cout<<matrix[i][j];

            }
            cout<<'\n';
        }
    }

    void transpose(int arr[][maxcols], int rows, int cols){

        int result[maxcols][maxcols];

    for(int i=0;i<rows;i++){
        for(int j=0;j<cols;j++){
              result[i][j]=arr[j][i];




        }
        cout<<endl;
    }
    printmatrix(result, rows, cols);
    }

int main(){
    int rows;
    cin>>rows;
    int cols;
    cin>>cols;
    int arr[maxcols][maxcols];

    for(int i=0;i<rows;i++){
        for(int j=0;j<cols;j++){
            cin>>arr[i][j];

        }
        cout<<endl;

    }

    transpose(arr, rows, cols);
    
    return 0;

}
